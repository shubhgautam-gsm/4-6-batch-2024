#include <stdio.h>
#include <stdbool.h>  // Import the boolean header file 

int main() {
  bool FAN = true;
  bool TV = false;
  printf("%d\n", FAN);  // Returns 1 (true)
  printf("%d", TV);         // Returns 0 (false)
  
  return 0;
}