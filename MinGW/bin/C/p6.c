#include <stdio.h>

// Function to add two numbers
int add(int a, int b) {
   return a + b; // Return the sum of a and b
}

int main() {
    int total = add(5, 6); // Call the add function and store the result in total
    printf("Total is %d\n", total); // Print the result
  
}
